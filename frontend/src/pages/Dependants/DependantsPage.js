import React from 'react';
import { Container, Typography } from '@mui/material';

const DependantsPage = () => {
  return (
    <Container maxWidth="lg">
      <Typography variant="h4">Dependants</Typography>
    </Container>
  );
};

export default DependantsPage;
